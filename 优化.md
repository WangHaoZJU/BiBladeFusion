# BiBladeFusion 未知叶片真机流程性能优化任务书

## 1. 文档目的

本文档供后续 agent 在当前 BiBladeFusion 项目中实施性能优化时使用。目标仅是减少未知叶片三视图 bootstrap、粗扫和精扫流程中的重复计算、重复 I/O 与同步阻塞；不得通过降低科学质量、安全门槛或证据完整性换取速度。

开始修改前，agent 必须重新检查当前 `git status --short`、当前提交、`configs/local.yaml` 的实际解析结果以及是否仍有真机任务运行。当前工作区已有用户/前序任务的未提交修改，禁止使用 `git reset --hard`、`git checkout --`、批量格式化或覆盖这些修改。

当前真机实验资产必须视为 append-only 证据：

```text
data/experiments/blade-placement-20260901-01-attempt-09
```

禁止删除、覆盖、重命名或手工修补该目录中的任何文件。性能回放必须把输出写到全新的目录，不能把 benchmark 产物写回实验目录。

## 2. 不可改变的边界

以下内容不属于性能优化授权范围：

- 不修改 `configs/local.yaml`、`configs/default.yaml` 或任何安全/科学阈值。
- 不修改 FoundationStereo 权重、模型配置、推理迭代数、精度模式或输入分辨率。
- 不修改标定、MDH、STL、碰撞模型、workspace、禁入 AABB 或机器人连接配置。
- 不降低 `minimum_source_views=3`、`maximum_source_views=3`、`minimum_free_observations=3`。
- 不把 `UNKNOWN` 当作 `FREE`，不扩大 `accepted_unknown` 的语义或范围。
- 不降低 stationarity、反馈新鲜度、连续碰撞、IK、FK、相机间隙或路径短段证明门槛。
- 不绕过 `IDLE`、stop-and-capture、人工 `EXECUTE ...` token 或现场检查。
- 不改变事件链的 fail-closed 行为，不把失败状态改写为完成。
- 不让 read-only GUI、性能 worker 或缓存获得机器人命令、批准或执行能力。

任何优化若无法证明与旧实现的安全/科学结果等价，必须停止在实验分支，不得进入真机流程。

## 3. 当前真实基线与证据等级

### 3.1 数据来源

基线来自 `blade-placement-20260901-01-attempt-09` 的真实三视图运行：

- 权威阶段总时长：`runs/coarse/events/*.json` 中的事件时间和 `perception_cycle_duration_s`。
- 子阶段时间：各不可变产物的 filesystem mtime，仅用于定位热点，不视为独立 timing acceptance。
- 数据规模：实际 `.json`、`.npy`、occupancy mapping 和 live timeline 文件大小。
- 调用链：当前源码直接检查。当前仓库没有 GitNexus 索引，因此不要把未验证的调用关系写成已证实事实。
- 没有对正在运行的真机进程使用侵入式 profiler；优化前必须先补充低扰动 timing span 或离线 replay benchmark。

### 3.2 三视图权威时长

| 视图 | `perception_cycle_duration_s` | `foundation_stereo_completed` 到地图状态提交 | 地图状态提交到 coarse `generation.json` |
|---|---:|---:|---:|
| `operator_bootstrap_000` | 531.567 s | 141.816 s | 145.854 s |
| `operator_bootstrap_001` | 991.799 s | 302.460 s | 500.410 s |
| `operator_bootstrap_002` | 1406.192 s | 433.792 s | 1038.734 s |

三个 `stereo_inference` 目录都在 `foundation_stereo_started` 后约 3–4 秒产生主要结果。该 mtime 只能说明本次运行中 CUDA 网络前向不是十几分钟延迟的主体，不能代替独立 GPU profiler。

### 3.3 随来源数增长的数据规模

| 指标 | 第1帧 | 第2帧 | 第3帧 |
|---|---:|---:|---:|
| occupancy mapping 目录 | 约 42.7 MB | 约 85.3 MB | 约 133.7 MB |
| stationarity 样本数 | 7,745 | 14,030 | 19,809 |
| 单份 stationarity JSON | 约 7.4 MB | 约 13.5 MB | 约 19.1 MB |
| occupied voxel 数 | 3,222 | 5,034 | 7,156 |
| free voxel 数 | 0 | 0 | 24,289 |
| coarse reconstructed base points | 51,259 | 18,060 | 120,219 |

每个 cycle 同时保存 `inference_stationarity_trace.json` 和 `inference_stationarity.json`，因此 stationarity 的实际写入与后续哈希/重读量约为表中单份大小的两倍。

### 3.4 当前运行已经证明的事实

- 第三视图的权威事件已经达到 `map_ready`，三个 `source_view_ids` 不同。
- `coarse_science/generations/000002/generation.json` 已生成。
- 主要延迟发生在 stereo 结果产生之后的 occupancy/source replay、coarse science、stationarity 证据、完整 readback/checkpoint 和 live snapshot 路径。
- 第二、第三帧耗时随来源窗口增长，存在明显的重复回放/重复验证模式。
- 当前证据尚不足以断言某一个 Python 函数占用了全部 CPU；必须先插桩再决定具体重构范围。

## 4. 当前调用链与候选热点

### 4.1 Perception 和 occupancy source-window 回放

主要入口：

```text
FoundationStereoOccupancyCycleEngine.infer_and_update
  -> infer_rectified_stereo
  -> prepare_foundation_stereo_occupancy_frame / hard ROI preflight
  -> _fresh_rebuild_sources
  -> _rebuild_updates
  -> write_occupancy_mapping
  -> read_occupancy_mapping
  -> coarse science asset preparation
  -> stationarity trace finish/write/readback
```

相关源码：

- `src/biblade_fusion/workflows/foundation_stereo_cycle.py`
- `src/biblade_fusion/workflows/occupancy_mapping.py`

`_rebuild_updates()` 明确按顺序遍历全部 source；新的 cycle 还会把历史来源对应的 depth、confidence、mask 和 mapping snapshot 再写入新的 `occupancy_mapping` 目录。这是“数据规模约随1/2/3帧增长”的已证实原因之一。

### 4.2 Stationarity 证据正反馈

robot-state sampler 从 capture 后持续运行到 perception/science 处理结束。处理越慢，trace 越长；trace 越长，JSON 序列化、哈希、严格解析和 readback 又越慢。

相关源码：

- `src/biblade_fusion/workflows/foundation_stereo_cycle.py`
- `src/biblade_fusion/storage/inference_stationarity.py`
- `src/biblade_fusion/devices/robot/elite_rtsi_sampler.py`

不得通过降低采样频率、丢弃 gap、截断 trace 或放宽 `maximum_feedback_interval_s` 解决该问题。

### 4.3 Coarse generation 和 coverage

主要入口：

```text
CoarseScienceSession.accept_cycle
  -> accept_prepared_view
  -> append_coarse_scan_generation
  -> read initialization / plan / previous generation / prior coverage
  -> update_coverage
  -> write coverage ledger
  -> write generation
```

相关源码：

- `src/biblade_fusion/workflows/unknown_blade_coarse.py`
- `src/biblade_fusion/storage/coarse_scan.py`

第三帧从 `map_ready` 到 `generation.json` 约 17 分钟，是当前最严重的单段延迟。是否主要耗在 `read_coarse_scan_generation()`、coverage 更新、数组哈希或 source readback，必须由 Phase 0 timing span 确认。

### 4.4 Experiment handoff 的重复完整重读

当前调用链包含：

```text
UnknownBladeSupervisedRuntime._append_coarse_checkpoint
  -> UnknownBladeExperimentWriter.append_coarse_checkpoint
     -> read_stop_scan_run
     -> read_coarse_scan_generation
  -> _verify_handoff_chain
     -> read_unknown_blade_experiment
```

相关源码：

- `src/biblade_fusion/workflows/unknown_blade_runtime.py`
- `src/biblade_fusion/storage/unknown_blade_experiment.py`

同一 checkpoint 在 append 前后存在多层严格重读。完整重读对恢复、粗精交接和最终完成很重要，但不应在同一事务内无依据地重复多次。

### 4.5 Live supervision 同步阻塞

主要入口：

```text
LiveSupervisionBridge.observe_perception
LiveSupervisionBridge.publish_status
  -> occupancy voxel centres
  -> display union / registry verification
  -> collision mesh base transform
  -> copy/write arrays and bound assets
  -> commit supervisory snapshot
```

相关源码：

- `src/biblade_fusion/supervision/live.py`
- `tests/unit/supervision/test_live.py`

已观察到第一、第二个 coarse checkpoint 到 live snapshot 分别约 1分40秒和5分03秒。live snapshot 中还包含约 48 MB collision vertices 和约 16 MB collision triangles。它是 read-only 功能，但目前同步运行在主状态机回调中，因此完成前控制台不会返回下一条操作提示。

## 5. 实施原则

1. **先测量，再修改。** 不得仅凭文件大小或函数名直接重构。
2. **一次只优化一个边界。** 每个阶段单独 commit、单独 benchmark、单独等价性报告。
3. **旧实现是 oracle。** 新实现必须对同一离线输入与旧实现逐字段或逐数组比较。
4. **缓存必须由不可变 authority 绑定。** 不能只按路径、mtime 或对象身份缓存。
5. **关键边界仍需独立 readback。** 缓存不能替代 motion approval、MAP_READY publication、schema-5 handoff、resume 和 final completion 的权威重读。
6. **不得静默降级。** 缓存不一致、worker 失败、输出不等价或 timing 缺失都必须 BLOCKED/FAILED，而不是回退到宽松结果。
7. **避免实时调度回归。** 后台 worker 不得使用 `SCHED_FIFO`；并发数必须有界，不能抢占 RTSI sampler 或制造新的 feedback gap。

## 6. 分阶段优化方案

### Phase 0：非侵入式精确计时和离线基线

这是必须先完成的阶段，不得跳过。

要求：

- 为以下边界增加 `time.monotonic_ns()` span，统一记录小型、非安全 authority 的诊断数据：
  - stereo backend；
  - stereo artifact write/readback；
  - current-frame occupancy prepare；
  - historical source replay；
  - occupancy write/readback；
  - foreground/proxy support；
  - coarse reconstructed view；
  - coarse scan view；
  - sampler finish与stationarity write/readback；
  - generation coverage update/write/read；
  - experiment checkpoint append/full verify；
  - live perception ingest、collision geometry、voxel conversion、asset write、snapshot commit；
  - IK/FK candidate filtering和segment preflight。
- 诊断 span 不能参与 safety hash、approval token 或科学输出。
- instrumentation 必须 O(1) 内存，不写大型 trace，不在采样热路径逐样本打印。
- 优先建立读取 `attempt-09` 的离线 replay/benchmark；输出到全新临时目录。
- 至少记录 cold run 3 次、warm run 5 次、p50/p95、CPU time、peak RSS、磁盘读写字节和产物大小。

Phase 0 完成条件：能够用数据回答每个大阶段内部哪三个函数占时最多。未达到该条件，不得开始 occupancy 或 schema 级重构。

### Phase 1：事务内 verified readback/hash 去重

风险：低到中。建议首先实施。

方向：

- 在单个进程、单个不可变事务内复用已严格验证的 typed object。
- 缓存 key 至少包含：resolved path、expected SHA-256、size，以及该 artifact 的 schema/kind/version。
- 文件 identity 或内容发生变化时立即失效并 fail closed。
- 不做跨进程、跨 resume 的未验证缓存。
- 查找同一调用链中重复的 `read_*()`、`json.loads()`、`np.load()` 和 `_sha256()`；每项删除前必须证明上游已经验证同一 authority。
- checkpoint append 后不要无条件完整验证同一链两次；保留一次增量 head 验证，并在 resume、schema-5 handoff、final completion 执行完整重放。

禁止：

- 全局按路径 `lru_cache`；
- 以 mtime 作为权威；
- 跳过 commit 前 TOCTOU 检查；
- 对可变 input/config 使用缓存。

### Phase 2：Live supervision 移出关键同步路径

风险：中。收益大且不应触碰运动算法。

推荐设计：

- 主线程向有界队列提交 detached、immutable snapshot input；单个 `SCHED_OTHER` worker 完成可视化计算和写盘。
- 队列必须有界，不能因 GUI 落后而无限占用内存。
- 允许合并仅供显示的中间状态，但不得丢弃 `MAP_READY`、`WAITING_APPROVAL`、`BLOCKED`、`FAILED`、`COMPLETE` 等权威状态的可视化发布。
- 在打印任何 motion approval token 前设置 barrier：当前 occupancy binding、prepared segment 和现场应查看的 GUI snapshot 必须已成功提交。
- worker 异常必须锁存为 BLOCKED；不得忽略异常继续给 token。
- 静态碰撞网格、manifest、calibration 等按内容 hash 只写一次；后续 snapshot 引用 immutable asset。
- 优先存每个 link 的静态 mesh和当前 link transform，避免每帧重复写完整 base-frame union mesh。变更前必须确认 replay GUI 能得到完全相同的几何显示。
- 不能让 worker 持有 robot、executor、approval callback 或命令接口。

### Phase 3：Stationarity 流式验证与紧凑存储

风险：中到高。必须保持完整证据。

方向：

- sampler 继续保留每个原始样本，不降低频率、不截断时段。
- 在采样进程中在线累计 maximum gap、joint/TCP delta、controller lag 等统计，同时仍保存完整 trace。
- 将大量数值样本从 JSON 对象数组迁移到固定 dtype 的 `.npy` 或项目已有的确定性二进制格式；小型 JSON metadata 保存 schema、shape、dtype、单位、来源和文件 SHA-256。
- reader 必须验证 dtype、shape、有限值、单调时钟、source binding和完整文件 hash。
- 保留旧 schema reader；新 writer 的迁移需单独 schema version，不能悄悄改变既有格式。
- 新旧格式对同一 trace 的 stationarity 结论、最大 gap、最大位姿变化和阻塞原因必须完全一致。

禁止：只保存摘要、丢弃完整 trace、降低证据频率或缩短 evidence 覆盖区间。

### Phase 4：Occupancy per-source contribution 缓存与确定性合并

风险：高，属于运动安全关键路径。只能在 Phase 0–3 稳定后实施。

方向：

- 对每个已接受来源保存并哈希绑定其 self-mask、valid mask、ray/free contribution、occupied contribution和相机几何。
- 初始 bootstrap 1→2→3帧只计算新来源贡献，再按固定顺序确定性合并。
- 滚动三来源窗口不能直接从聚合 map 中“近似减去”旧来源；必须用三个独立来源贡献重新确定性合并。
- 旧 `_rebuild_updates()` 保留为测试 oracle；针对真实 `attempt-09` 和合成边界场景运行 shadow A/B。

必须完全一致的输出：

- source 顺序、view identity和独立性判定；
- `occupied_indices`、`free_indices`、`free_observation_counts`；
- `UNKNOWN/FREE/OCCUPIED/accepted_unknown` 语义；
- `minimum_free_observations`、方向/平移独立性证据；
- parent/quality/content/evidence hash链；
- `map_state`、`state_reason`、motion eligibility；
- robot self-mask和静态自由区处理。

只要有一个 voxel、计数、理由或 hash 不一致，新路径就不得启用。

### Phase 5：Coarse generation/coverage 增量化

风险：高，影响科学目标选择和 schema-5。

方向：

- generation 只追加当前 view authority和上一 generation head，不重复加载所有历史点云。
- coverage ledger 对当前 observation 做 delta 更新；上一 ledger 必须是已经严格验证的不可变对象。
- 历史 view 仍通过 generation hash链可完整回放。
- schema-5 handoff、resume 和最终 coarse completion 继续执行全量严格回放。
- 旧全量实现作为 oracle，对每一代比较完整 coverage和下一目标选择。

必须完全一致的输出：

- proxy support mask、PCA/frame和双侧 proxy；
- patch定义、coverage bin/count和observation ID；
- front/back side、鳍片相反斜视证据；
- next-view候选、排序、IK/FK过滤结果；
- coverage complete与schema-5 transition判定；
- schema-5内容和最终reference binding。

### Phase 6：有界并行化

只在前述去重完成后考虑；不能用并行掩盖重复工作。

- 可并行项仅限相互独立的只读 hash、asset readback 或显示数据转换。
- worker 数量必须有界；默认从1–2个后台 worker开始基准。
- 不硬编码当前主机 CPU ID，不让普通 worker 使用实时调度。
- 每次并行改动必须重新运行长负载 RTSI sampler，确认 maximum sample gap、controller lag和stationarity结果不回归。
- 不允许并行写同一 append-only generation、event或snapshot目录。

## 7. 明确不应优先做的“优化”

- 安装 xFormers/FlashAttention：本次真实运行中 stereo 主要产物约3–4秒出现，预期总收益很小。
- 修改 CUDA、混合精度、模型权重或输入分辨率：会改变科学结果，超出范围。
- 提高 stationarity gap、降低三视图/三票门槛：这是放宽安全策略，不是性能优化。
- 删除严格 readback：应做的是消除同一事务内的重复验证，而不是取消关键边界验证。
- 盲目把全部步骤多进程化：可能重新制造已发生过的 robot feedback gap，并增加5–10 GB模型/数组复制。
- 优先优化 KDL IK warning：目前没有 timing span 证明 IK 是本轮主要瓶颈。

## 8. 等价性和回归测试要求

优先复用现有测试文件：

```text
tests/unit/workflows/test_foundation_stereo_cycle.py
tests/unit/workflows/test_foundation_stereo_fork.py
tests/unit/workflows/test_occupancy_mapping.py
tests/unit/workflows/test_unknown_blade_coarse.py
tests/unit/workflows/test_unknown_blade_runtime.py
tests/unit/storage/test_inference_stationarity.py
tests/unit/storage/test_occupancy_mapping_asset.py
tests/unit/storage/test_coarse_scan.py
tests/unit/storage/test_unknown_blade_experiment_chain.py
tests/unit/supervision/test_live.py
tests/unit/robotics/test_stationarity.py
tests/unit/mapping/test_occupancy.py
```

最少验证命令：

```bash
/usr/bin/env -u PYTHONPATH PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  .venv/bin/python -m pytest -q \
  tests/unit/workflows/test_foundation_stereo_cycle.py \
  tests/unit/workflows/test_occupancy_mapping.py \
  tests/unit/workflows/test_unknown_blade_coarse.py \
  tests/unit/workflows/test_unknown_blade_runtime.py \
  tests/unit/storage/test_inference_stationarity.py \
  tests/unit/storage/test_unknown_blade_experiment_chain.py \
  tests/unit/supervision/test_live.py
```

阶段性测试通过后再运行完整测试。测试通过仅证明软件结构行为，不等于真机 acceptance。

## 9. 性能验收门

每个优化阶段都必须提交一份 before/after 报告，至少包括：

- 同一 commit基线、同一配置、同一离线输入、同一主机；
- cold 3次、warm 5次；
- 每个 span 的 p50/p95和CPU time；
- peak RSS、GPU memory、磁盘读写字节、总产物大小；
- 完整语义等价比较结果；
- 异常/失败路径和恢复路径测试结果。

合入条件：

1. 所有安全、科学、事件链和目标选择结果等价。
2. 目标阶段 p50 有明确、可重复的改善；若改善不足以抵消复杂度，不合入。
3. 非目标关键阶段 p95 不得出现显著回归。
4. peak RSS、磁盘占用或后台积压若上升，必须给出上限和收益证据；不得无界增长。
5. RTSI maximum sample gap、stationarity和停止证据不得变差。
6. 任何异常仍然 fail closed，且失败证据完整保留。

不要预先承诺绝对分钟数。完成 Phase 0 后，以真实 cold/warm baseline设定量化目标；优先追求去除随 source 数增长的重复工作，使第2/3帧不再近似按2倍/3倍增长。

## 10. 真机前最终验收顺序

1. 离线 replay 与旧实现逐字段/逐数组等价。
2. 全测试通过。
3. 目标主机无机器人运动的长负载 sampler 测试通过。
4. `scan doctor --mode unknown --experimental` 返回0且无 FAIL。
5. 新 placement、新 run_id、新 output 做三视图 stop-and-capture；不得复用或覆盖失败资产。
6. MAP_READY 后仅验证首次 `Prepared segment` 生成，不自动输入 token。
7. 由操作员确认 GUI、现场、碰撞方向和电缆后，才能进行受监督短段运动。

## 11. 建议交给 agent 的第一项任务

不要一次实现 Phase 1–6。第一项任务限定为：

1. 只读审计上述真实基线和当前调用链；
2. 实现 Phase 0 的低扰动 timing span/离线 benchmark；
3. 输出按 inclusive/exclusive CPU wall time 排序的热点报告；
4. 从 Phase 1 或 Phase 2 中选择一个单一边界提出最小改动；
5. 在得到用户明确同意前，不修改 occupancy合并、stationarity schema、coarse coverage算法或motion approval路径。

这样可以先确认最值得优化的真实热点，再实施小范围、可回滚、可证明等价的效率改动。
