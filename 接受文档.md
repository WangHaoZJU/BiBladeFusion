# BiBladeFusion 跨主机接收、验收与后续工作文档

> 更新日期：2026-08-29（Asia/Shanghai）
> 用途：将 BiBladeFusion 迁移到 GPU 主机后，作为新主机上 Codex 的首要上下文、环境验收清单和后续工作边界。
> 适用项目：`BiBladeFusion: A Robot-Guided Bilateral 3D Geometry and Thermal Reconstruction System for Thin-Walled Blades`

## 0. 给新主机 Codex 的首要指令

在修改代码、运行真机命令或生成新实验资产之前，必须：

1. 完整阅读本文档、[README](README.md)和[未知叶片监督式闭环实验](docs/supervised-blade-experiment.md)。
2. 在仓库根目录执行`git status --short --branch`、`git rev-parse HEAD`和`git submodule status --recursive`。
3. 区分“代码可用”、“环境可用”、“感知可用”和“物理运动已验收”，不得将前三者误称为最终真机验收。
4. 在`scan doctor --mode unknown`通过且用户明确表示人已在场、低速、可直接急停之前，不得启动任何可能运动的入口。
5. 不得为了让 doctor 变绿而猜测工作距离、工作空间、碰撞尺寸、短段上限、时序上界或验收 ID。
6. 对写一次数字资产使用新路径，不删除、覆盖或手工篡改原始 session、推理、验收和实验链。
7. 每项代码任务完成后：更新开发日志，运行与风险匹配的测试，检查 diff，单独提交并推送。

## 1. 项目目标与已固化的技术决策

### 1.1 项目目标

使用 ES68 六轴机械臂带动末端 Intel RealSense D435i，对未知薄壁叶片完成：

- 正、反两侧三维几何采集；
- 每个主表面上一只突出鳍片的独立发现、分区和精扫；
- 前缘、后缘、叶尖、叶根、鳍片面、鳍片根部和鳍片自由边的覆盖评价；
- 基于实际粗扫表面法向、可见性、曲率和区域质量的下一视点选择；
- 基于增量三态占用图、机器人 STL/FCL 和连续扫掠证明的受控短段运动；
- 最终双面 TSDF/网格重建及可追溯的实验资产链。

热像仪与温度场融合属于后续阶段。当前不应让热像功能阻塞几何闭环验证。

### 1.2 不得随意改变的决策

- **不再使用 PC-NBV**。视点规划基于未知叶片的粗扫几何、论文启发的真实曲面分区、可见性和质量闭环。
- **实际机器人型号是 ES68，不是 CS68**。部分`elite_cs`资源目录名是HoloRobot/Elite CS系列来源兼容命名，不能据此改写机器人型号。
- **FoundationStereo 是唯一正式深度后端**。RealSense原生深度可用于独立对比，不是运行中的降级后端。
- FoundationStereo 输入为用户标定参数矫正的D435i左、右红外图，左红外相机是重建和手眼坐标主相机。
- 双目内外参求解不使用D435i厂家内参；使用ChArUco、张正友初值和联合BA。
- 手眼主变换是`flange_T_left_ir`；求解使用控制器关节角经HoloRobot对齐的ES68 FK生成`base_T_flange`，RTSI TCP只用于验证。
- 坐标命名遵循`parent_T_child`，长度使用米，角度内部使用弧度，只在人机界面/命令明确标注时使用度。
- 占用图与科学重建消费同一 FoundationStereo 深度身份，不得在一次运行中无声切换深度语义。
- 运动策略是“停站采集—GPU推理—占用更新—规划一段—证明一段—人工批准一段—执行一段—再停站采集”，不要求 FoundationStereo 跟随 ServoJ 实时运行。
- 未知空间和已占用空间均阻塞运动；三态占用图是停站静态证据，不是安全等级的连续动态避障。
- 叶片两个宽面在物理上计划大致朝向机械臂基坐标系左右，以增大可达区域；但正反侧语义仍由第一视图和双侧拓扑链绑定，不硬编码为全局左/右。
- 两侧各恰有一只突出鳍片；鳍片发现必须失败关闭，不得把鳍片混入主叶面轮廓。
- 按用户当前的实验假设，线缆暂不纳入碰撞模型；这是实验范围假设，不得表述为线缆风险已被证明为零。
- 热像当前保持`enabled: false`。

## 2. 系统的实际运行链

```text
人工安全初始位姿
  → 显式stop与停稳证明
  → D435i左/右红外原始帧 + 采集前后两次ES68状态
  → 用户标定参数双目矫正
  → FoundationStereo视差、左右一致性、置信和度量深度
  → ES68 FK · flange_T_left_ir注册到base
  → 机器人自遮罩
  → 三态占用图（FREE/OCCUPIED/UNKNOWN）
  → 粗扫多视图融合和双侧分离
  → 改进Angle Criterion边界 + 四曲线拟合 + 精确裁切
  → 主表面/前后缘/叶根/叶尖/两只鳍片分区
  → OBB中心、主法向、曲率自适应分块
  → 基准距离 + 区域自适应精扫候选
  → 工作空间/IK/FK/可见性筛选
  → 连续STL/FCL和连续占用扫掠证明
  → 精确人工approval token
  → 一个受控ServoJ短段
  → 再stop、停稳、采集和质量闭环
  → 双面TSDF/网格与最终覆盖封存
```

下一视点不是简单最近点。选择器优先处理未完成或低质量的拓扑区域，再比较投影覆盖、入射角、遮挡、工作距离、可达性和精确重采历史。关节行程仅作平局条件，不得牺牲可达性、安全或科学质量。

## 3. 已完成的软件能力

截至功能基线`3e4231ac0eb8fe7e14b0903e5ba25fd2e39fc94e`：

- D435i左右红外 ChArUco + 张正友初值 + 联合双目BA标定GUI；
- 界面先启动，用户点击开始后才采集，采集与角点求解分离；
- 留一验证、标定质量门和活动标定配置发布；
- ES68左红外手眼标定GUI，Park-Martin初值 + LM BA，求解使用关节角经HoloRobot标定FK得到`base_T_flange`；
- RTSI TCP只是运行兼容性/验证信号，不是手眼求解输入；
- 同步D435i原始帧与ES68状态数字资产；
- FoundationStereo严格单后端适配、doctor、保存session推理和不可变推理资产；
- 粗扫多视图集合、机器人位姿强先验和同侧有界ICP；
- 双侧真实曲面分离、边界拟合、曲面分块、鳍片分区和真实法向视点；
- 双面独立稀疏TSDF和网格提取，避免薄壁正反面在同一截断带内互相抹消；
- 基于真实曲面距离、入射角、法向、RMSE、孔洞和拓扑区域的覆盖质量账本；
- 粗扫到schema-5再到精扫的不可变交接链；
- ES68+D435i跟踪的最终STL装配manifest和离线PySide6检视工具；
- Pinocchio/HoloRobot FK/IK、STL/FCL连续碰撞证明、占用体素连续扫掠证明；
- 机器人自遮罩、三态占用图、至少三个独立人工初始视角和滑动新鲜窗口；
- 停—算—规划—短移协调器、一次性permit、精确approval token、段后自动采集；
- 只读类RViz监督界面，显示机器人、轨迹、占用图、融合点云、目标视点和事件时间线；
- 静态自由区、运动包络、几何科学和运行时序的失败关闭验收资产；
- 未知叶片公开入口`bbf scan run-unknown`及严格单实验根恢复。

当前离线回归基线：

```text
986 passed in 248.73s
Ruff: PASS
compileall: PASS
bootstrap shell syntax: PASS
uv.lock check: PASS
```

## 4. 已有真机数据与标定质量

### 4.1 D435i红外双目

活动资产：`data/calibrations/d435i_ir_active.yaml`

```text
分辨率：1280 × 720
左相机单目 RMS：0.098191 px
右相机单目 RMS：0.074901 px
联合双目 RMS：0.086730 px
极线 RMSE：0.100748 px
极线 P95：0.226316 px
基线：0.049990309 m
工厂内参：未使用
畸变模型：Brown 5参数
```

SHA-256：

```text
0d51ef13733b79f4fa38b8a8cdb632fd944f95313533fdc60e7db7eeb0864246
```

该标定只能和1280×720运行链直接绑定。不得在640×480下继续使用同一数值内参。

### 4.2 ES68—左红外手眼

活动资产：`data/calibrations/es68_left_ir_hand_eye_active.yaml`

```text
方法：OpenCV Park-Martin + LM bundle adjustment
主变换：flange_T_left_ir
样本数：40
平移 RMSE：0.000441946 m
旋转 RMSE：0.138503 deg
BA最终重投影 RMSE：0.452957 px
独立验证：已通过，5个留出样本
独立验证重投影 RMSE：0.734689 px
```

SHA-256：

```text
a80ebfe72d4911cd15a6c3907a1908803eb60f7e27c2e65eb1fc8d2594b35c2d
```

换电脑不会本质改变这两项物理标定。只有同一台ES68、同一台D435i、同一连接件且相机装配完全未拆动时才能复用。如果相机或连接件重装，必须重做手眼标定；更换D435i还必须重做双目标定。

### 4.3 静态多视图重合

`data/validations/native_overlap_20260828_fk_authority_v2`已通过五视图 FK 权威链静态一致性验证。其主指标是未使用ICP修正的对称投影深度残差。该结果证明内部静态一致性，不等价于可溯源的绝对尺寸精度验收。

## 5. 尚未完成、不得误报为已完成的事项

1. 目标GPU主机上的FoundationStereo安装、完整checkpoint加载、1280×720实际推理、显存和冷/热延迟验证。
2. 最终ES68+D435i实物尺寸、STL尺度/原点/方向/装配姿态/保守余量验收。
3. 目标workcell范围、叶片支架/夹具语义和静态自由AABB验收。
4. ServoJ短段上限、速度缩放、跟踪误差、stop漂移、状态新鲜度和六类停稳速度信号的物理验收。
5. 机器人自遮罩在真实D435i图像上的召回率和对合法自由空间的影响。
6. 已知安全和已知碰撞轨迹上，连续网格/占用扫掠证明的假阴性和假阳性验收。
7. 叶片、两只鳍片、鳍片根部、自由边、前后缘、叶尖和叶根的真机前景掩模及分区质量验收。
8. 双面表面RMSE、厚度、法向、孔洞、鳍片双面和拓扑覆盖的几何科学验收。
9. 新GPU/OS/驱动/Python环境的science runtime contract。
10. 目标GPU、控制器、workcell和同一Linux boot下的四类运行时序验收。
11. 真实未知叶片从人工三视角开始，经粗扫、schema-5、精扫到`FINE_COMPLETED`的完整闭环实验。
12. 红外热像仪驱动、标定、时间同步、可见性、温度投影和多视图温度融合。

因此，当前准确状态是：**未知叶片闭环的验收前软件链已完成；物理发布和最终实验尚未完成。**

当前源主机上没有已完成的`data/acceptance/`目录。不得假设静态自由区、运动包络、科学精度或运行时序已经获得发布权威。

## 6. 源码与必须单独迁移的数字资产

### 6.1 Git基线

```text
远程：git@github.com:WangHaoZJU/BiBladeFusion.git
分支：main
功能代码基线：3e4231ac0eb8fe7e14b0903e5ba25fd2e39fc94e
FoundationStereo子模块：6e8806816b533e4d13ddbb95ffa907b797060a62
```

`3e4231a`是添加本接收文档之前的功能代码基线。接收主机应使用包含本文档的最新`origin/main`，并在验收报告中记录它实际得到的完整HEAD；不要为了对齐这个历史功能哈希而回退、丢失接收文档或后续文档修正。

`git clone`会包含跟踪的ES68+D435i碰撞STL和活动manifest。不要从旧电脑再复制一份来覆盖跟踪文件。

### 6.2 Git不会带走的资产

`.gitignore`排除：

```text
data/
models/
outputs/
logs/
configs/local.yaml
.env
```

必须单独传输：

```text
data/                                  # 建议整体迁移
models/foundation_stereo/23-51-11/     # checkpoint + cfg.yaml
outputs/                               # 需回放旧派生资产时
logs/                                  # 需归档时
configs/local.yaml                     # 若已创建，迁移后仍要审查
elite_cs_sdk-1.0.0-cp312-cp312-linux_x86_64.whl
```

不要迁移：

```text
.venv/
.uv-cache/
.pytest_cache/
__pycache__/
```

当前源主机没有`configs/local.yaml`，也没有FoundationStereo checkpoint/`cfg.yaml`。必须从已安装过的GPU主机或官方模型包取得完整`23-51-11`目录。

### 6.3 迁移完整性

对`data/`、`models/`、SDK wheel和本地配置生成SHA-256清单，在新主机上从仓库根目录执行`sha256sum -c`。

当前已知摘要：

```text
0d51ef13733b79f4fa38b8a8cdb632fd944f95313533fdc60e7db7eeb0864246  data/calibrations/d435i_ir_active.yaml
a80ebfe72d4911cd15a6c3907a1908803eb60f7e27c2e65eb1fc8d2594b35c2d  data/calibrations/es68_left_ir_hand_eye_active.yaml
8db4db6e7fd96d45b99615106c2a2c3dd9d877c8bba73e7feac879e2eca44d03  elite_cs_sdk-1.0.0-cp312-cp312-linux_x86_64.whl
```

如需完整重放旧派生资产，优先保持与旧主机相同的绝对仓库路径。如果新路径不同，不得手工修改不可变JSON/YAML中的路径或摘要；应从原始session在新路径重新生成派生资产。

## 7. 新主机安装顺序

### 7.1 平台要求

- Ubuntu 22.04/24.04 x86_64；
- Python 3.12，项目要求`>=3.12,<3.13`；
- NVIDIA驱动且`nvidia-smi`成功；
- 本地图形会话，用于PySide6标定/监督/STL界面；
- D435i的USB 3链路与RealSense udev权限；
- ES68网口与同网段IP。

项目锁定PyTorch 2.4.1及CUDA 12.1用户态依赖。通常不需要另外安装完整CUDA Toolkit，但驱动必须与CUDA 12.1兼容。

### 7.2 克隆

```bash
git clone --recurse-submodules \
  git@github.com:WangHaoZJU/BiBladeFusion.git \
  biblade-fusion

cd biblade-fusion
git switch main
git pull --ff-only
git submodule update --init --recursive
git rev-parse HEAD
git submodule status --recursive
```

迁移接收时保持在最新`main`。只有进行独立历史复现且不影响接续工作时，才在另外worktree中检出`3e4231a`。

### 7.3 本地配置不是增量覆盖

`load_settings()`会把指定YAML当作完整`AppSettings`解析，所以必须：

```bash
cp configs/default.yaml configs/local.yaml
```

首先审查：

```text
robot.robot_ip
robot.local_ip
robot.sdk_wheel
realsense.serial_number
realsense.stereo_calibration_path
hand_eye.calibration_path
kinematics.model_path
foundation_stereo.repository_path
foundation_stereo.checkpoint_path
foundation_stereo.model_config_path
```

初始安装期间必须保持：

```yaml
robot:
  motion_enabled: false

occupancy:
  enabled: false

stop_and_capture:
  enabled: false

thermal:
  enabled: false
```

### 7.4 模型文件

```text
models/foundation_stereo/23-51-11/
├── model_best_bp2.pth
└── cfg.yaml
```

`model_config_path: null`表示使用checkpoint相邻的`cfg.yaml`。不得只复制`.pth`。

### 7.5 严格GPU安装

```bash
./scripts/bootstrap-gpu.sh \
  /absolute/path/to/elite_cs_sdk-1.0.0-cp312-cp312-linux_x86_64.whl \
  configs/local.yaml
```

脚本会创建Python 3.12`.venv`，使用`uv.lock`安装全部groups/extras，然后最后安装不在公开锁文件中的Elite SDK wheel。

后续如果重新执行精确`uv sync`，可能移除Elite SDK；之后必须再次安装wheel。正常验收命令优先使用`.venv/bin/...`。

`xformers`和`flash_attn`在本项目doctor中是可选加速警告，不是必需项。不要在未检查Torch/CUDA ABI前盲目安装。

## 8. 新主机分层验收命令

### 8.1 纯软件：不连硬件、不运动

```bash
git status --short
git diff --check
git submodule status --recursive

UV_CACHE_DIR=.uv-cache uv lock --check
bash -n scripts/bootstrap.sh scripts/bootstrap-gpu.sh

.venv/bin/python -m compileall -q src tests
.venv/bin/ruff check .
.venv/bin/pytest --collect-only -q
.venv/bin/pytest -q
```

预期收集并通过`986 tests`。它们是离线/模拟回归，不是GPU、相机或机械臂验收。

### 8.2 FoundationStereo doctor

```bash
.venv/bin/bbf version
.venv/bin/bbf doctor --config configs/local.yaml
.venv/bin/bbf stereo doctor --config configs/local.yaml
```

FoundationStereo必需项应为`PASS`：源码、checkpoint、`cfg.yaml`、Torch/torchvision、OmegaConf、timm、Hugging Face Hub、einops、SciPy、OpenCV和CUDA。

### 8.3 完整GPU推理烟测

doctor不会执行完整权重推理。使用已保存session：

```bash
.venv/bin/bbf stereo infer-session \
  --session data/20260827T140112.161541Z_overlap_00_reference_v2_66e37457 \
  --view-id overlap_00_reference_v2 \
  --config configs/local.yaml \
  --output outputs/gpu_smoke_001
```

`outputs/gpu_smoke_001`必须事先不存在。核对：

- checkpoint和`cfg.yaml`实际加载；
- GPU上实际分配显存，无OOM；
- 输出与1280×720标定链一致；
- 有效深度数大于0；
- 视差、左右一致性、深度和metadata完整；
- 记录冷启动与暖启动时间。

如果OOM，不得无声修改`scale`、`valid_iterations`或`hierarchical`。参数变化会改变科学合同，必须重做相关验收。

### 8.4 D435i只读检查

```bash
.venv/bin/bbf camera list --json

.venv/bin/bbf camera capture \
  --config configs/local.yaml \
  --no-emitter \
  --output data/hardware_validation/gpu_host_d435i_smoke.npz
```

校验`left_ir`和`right_ir`形状均为`(720, 1280)`。标定板采集时投射器应关闭；正式扫描投射器状态应由最终验收配置统一管理。

### 8.5 ES68只读检查

当前实验室地址：

```text
ES68控制器：192.168.6.60
旧主机网口：192.168.6.29/24
```

如果新主机使用`192.168.6.29`，必须确保旧主机不再同时使用该IP。

```bash
ip -br address
ping -c 4 192.168.6.60

.venv/bin/bbf robot status \
  --config configs/local.yaml \
  --ip 192.168.6.60 \
  --json
```

`robot status`不能运动机械臂。

### 8.6 导出这台控制器的MDH

```bash
.venv/bin/bbf robot export-kinematics \
  --config configs/local.yaml \
  --ip 192.168.6.60 \
  --output data/calibrations/es68_mdh_gpu_host.yaml
```

然后设置：

```yaml
kinematics:
  model_path: data/calibrations/es68_mdh_gpu_host.yaml
  joint_zero_offsets_rad: [0, 0, 0, 0, 0, 0]
```

该命令只读Primary接口，不加载任务、不松闸、不发轨迹。

### 8.7 相机—机器人同步采集

```bash
.venv/bin/bbf acquire snapshot \
  --config configs/local.yaml \
  --view-id gpu_host_sync_001 \
  --no-emitter
```

该命令只在D435i采集前后读取ES68状态，不发运动指令。随后将打印的session路径交给`stereo infer-session`完成新主机端到端感知烟测。

### 8.8 STL装配检查

```bash
.venv/bin/bbf robot inspect-model \
  --config configs/local.yaml \
  --joints-deg "0,-60,90,-60,-90,0"
```

该工具不连接机械臂或相机。视觉正确不等价于FCL或物理尺寸验收。

## 9. `configs/local.yaml`的真机填写边界

以[default.yaml](configs/default.yaml)完整复制为起点。

### 9.1 可根据已知身份填写

```text
robot.model = es68
robot.robot_ip = 192.168.6.60
robot.local_ip = 新主机实际网口IP
robot.sdk_wheel = 新主机的wheel绝对路径
realsense.serial_number = 目标D435i唯一序列号
realsense.infrared_width/height = 1280/720
realsense.stereo_calibration_path = data/calibrations/d435i_ir_active.yaml
hand_eye.calibration_path = data/calibrations/es68_left_ir_hand_eye_active.yaml
kinematics.model_path = 新导出的schema-2 MDH资产
foundation_stereo.repository_path = third_party/FoundationStereo
foundation_stereo.checkpoint_path = models/foundation_stereo/23-51-11/model_best_bp2.pth
foundation_stereo.model_config_path = null
foundation_stereo.device = cuda
thermal.enabled = false
```

### 9.2 只能在实测后填写

```text
proxy_model.estimated_thickness_m
view_planning.standoff_distance_m
view_planning.minimum_standoff_distance_m
view_planning.maximum_standoff_distance_m
view_filter.workspace
collision.link_radii_m
collision.camera_tool_radius_m
collision.minimum_joint_positions_rad
collision.maximum_joint_positions_rad
occupancy.workspace_bounds_min_m/max_m
occupancy.accepted_static_free_aabbs
stop_and_capture.maximum_segment_joint_delta_rad
四项stop_and_capture运行时上界
全部acceptance path/ID
```

`blade_foreground.enabled: true`只能与已绑定的schema-5参考链一起进入精扫。未知叶片入口会在粗扫完成后自己建立该链，不要用一份来源不明的外部粗模型跳过粗扫。

## 10. 运动发布前的四类不可变验收

### 10.1 静态自由区

从`configs/static_free_acceptance.template.json`复制声明，完成工作空间实测后记录：

```bash
.venv/bin/bbf safety record-static-free-acceptance \
  --declaration configs/static_free_acceptance.completed.json \
  --config configs/local.yaml \
  --output data/acceptance/es68_d435i_static_free_001
```

静态自由AABB不得覆盖叶片、支架、夹具或它们的可能包络。`OCCUPIED`永远优先于静态自由声明。

### 10.2 ServoJ运动包络

至少三次代表性低速试验，正反向交替执行同一封存短段，实测跟踪误差、stop漂移、反馈间隔和停稳信号，然后：

正式运动包络尚不存在时，只能使用专用commissioning入口取得这些物理数据。先对
`safety prepare-motion-envelope-trial`生成的封存候选做无硬件dry-run：

```bash
.venv/bin/bbf commission motion-envelope-trial \
  --candidate outputs/motion_envelope_commissioning_candidate_001 \
  --operator-id eiai \
  --output data/acceptance/commissioning_trial_001 \
  --config configs/local.yaml
```

只有候选/配置哈希、`0.02 rad`硬上限、连续网格扫掠证明和SDK所需的FIFO 99调度同时通过，命令才会
打印候选和唯一输出目录双重绑定的精确确认词。操作员在场、工作区净空且可急停时，原命令增加
`--execute --confirmation "<精确确认词>"`。反向试验增加`--direction reverse`，且会重新计算
反向连续网格扫掠证明。正式轨迹前固定执行`0.2 s`当前位姿ServoJ预热，完整的
`resume + 预热 + 轨迹 + 终点保持`受独立`3 s`deadline stop约束。该入口不使用陈旧占用图授权，仅用于最终
装配和人工净空下的低速物理commissioning；每次只执行一个封存候选，结果无论成功失败
都必须写入新目录。它不会设置`robot.motion_enabled: true`，也不会产生正式运动permit。
`writeIdle`后不得用单个RTSI速度样本判定停稳；必须在既定速度与终点误差阈值内取得完整连续
停稳窗口，并把窗口内及窗口建立前的采样轨迹写入commissioning证据。

```bash
.venv/bin/bbf safety record-motion-envelope-acceptance \
  --declaration configs/motion_envelope_acceptance.completed.json \
  --config configs/local.yaml \
  --output data/acceptance/es68_d435i_motion_envelope_001
```

验收资产会绑定STL、控制参数和连续证明合同。修改STL、速度、ServoJ周期或跟踪包络后必须重做。

### 10.3 几何科学验收

在最终GPU环境导出runtime contract：

```bash
.venv/bin/bbf safety science-runtime-contract \
  --config configs/local.yaml \
  --output data/acceptance/runtime_contract.json
```

证据顺序必须为：

```text
runtime contract
  → raw asset manifest
  → geometry evaluation
  → independent review
  → science acceptance
```

独立复核者必须不同于采集操作员。规范化和记录命令见[监督实验协议第7节](docs/supervised-blade-experiment.md#7-科学验收与schema-5时序预算)。

新GPU、驱动、OS、Python distributions、Torch/CUDA/cuDNN、源码、模型、标定或策略改变都会让旧contract失效。

### 10.4 运行时序验收

在目标GPU、目标workcell且同一次Linux boot中建立密封会话：

```bash
.venv/bin/bbf safety begin-runtime-timing-session \
  --config configs/local.yaml \
  --host-run-id bbf-timing-host-001 \
  --workcell-id es68-d435i-cell-001 \
  --output data/timing/measurement-session-001.json
```

至少三组唯一trial，同时包含cold和warm。每组必须各有：

```text
perception_cycle_trace
operator_reposition_trace
segment_execution_trace
schema5_handoff_trace
```

trace不能手填秒数，必须由`biblade_fusion.storage.measure_runtime_timing_trace()`包裹真实操作生成。当前刻意不提供一个可输入虚假秒数或代替操作员发起通用运动的CLI。

完整聚合与记录命令见[监督实验协议第7节](docs/supervised-blade-experiment.md#7-科学验收与schema-5时序预算)。

## 11. 正式运行前最终doctor

只有四类验收资产与`configs/local.yaml`完整绑定后，才可设置：

```yaml
robot:
  motion_enabled: true

occupancy:
  enabled: true

stop_and_capture:
  enabled: true
```

执行：

```bash
.venv/bin/bbf scan doctor \
  --mode unknown \
  --config configs/local.yaml
```

该命令不打开相机、不连接机械臂、不发运动、不生成permit。所有必需项的`FAIL`必须消失。最后保留`hardware acceptance`提醒性`WARN`是设计行为。

在验收未完成时，命令退出1是正确的失败关闭结果。

## 12. 完整未知叶片闭环运行

### 12.1 启动

操作员先用示教器把相机移到人工确认安全且可见叶片的初始位置。新实验目录必须不存在：

```bash
.venv/bin/bbf scan run-unknown \
  --config configs/local.yaml \
  --output data/experiments/unknown_blade_001 \
  --operator-id vale \
  --first-side front
```

首帧前景不唯一时，可用`--bootstrap-rectangle`和`component_hint`，或者用`--bootstrap-polygon`和`hard_roi`。两种提示不得同时使用。

另一终端只读监督：

```bash
.venv/bin/bbf supervise replay \
  --snapshot data/experiments/unknown_blade_001/live_timeline \
  --follow
```

监督界面不得批准、stop或控制机械臂；关闭监督界面不等价于急停。

### 12.2 交互规则

- 启动阶段不会自动连续采集；提示后每次按一次`c`才采一组。
- 初始需要至少三个彼此独立、人工确认安全的停站视角，用于建立初始占用图。
- 只改`view_id`不算独立视角；必须满足相机中心平移或光轴方向差异门限。
- 粗扫科学链默认至少正反面各三视图，与“建图至少三视图”不是同一个完成条件。
- 到达`MAP_READY`后，每次只规划和证明一个短段。
- 操作员必须原样输入该段唯一approval token；它不能授权下一段。
- 每段完成后必须显式stop和停稳，然后自动采一组。
- 进入精扫后，如果安全图缺新物理源，程序可要求操作员再按`c`补源；这一次不伪造科学覆盖。

### 12.3 状态链

```text
INIT
  → COARSE_CHECKPOINT+
  → PREPARED
  → FINE_START_CANDIDATE+
  → FINE_STARTED
  → FINE_CHECKPOINT*
  → FINE_COMPLETED
```

### 12.4 恢复

只能恢复用户明确指定的同一实验根：

```bash
.venv/bin/bbf scan run-unknown \
  --resume \
  --config configs/local.yaml \
  --output data/experiments/unknown_blade_001 \
  --operator-id vale
```

恢复不继承旧permit、approval、prepared segment、地图新鲜度或控制器运动权限。已经`FINE_COMPLETED`的实验恢复时只读返回，不连接设备。

## 13. 后续工作优先级

新主机 Codex 应依次推进：

### P0：迁移与GPU感知闭环

P0的首个交付物应是一份迁移验收报告，不是立即开发新功能。报告必须区分通过项、警告、失败项和尚未进行的物理验收。

1. 校验代码和子模块版本。
2. 校验非Git数字资产的SHA-256。
3. 建立`configs/local.yaml`，先保持所有运动功能关闭。
4. 运行严格GPU bootstrap。
5. 通过986项离线回归。
6. 用已保存session完成FoundationStereo实际CUDA推理。
7. 记录GPU、显存、驱动、Torch/CUDA/cuDNN、冷/暖推理耗时和输出质量。

### P1：不运动真机链

1. D435i枚举、1280×720左右红外采集。
2. ES68网络和只读RTSI状态。
3. 从这台控制器导出schema-2 MDH资产。
4. D435i + ES68同步快照。
5. 对新快照运行FoundationStereo，验证新主机端到端感知。
6. 离线审查STL装配和多关节位姿。

### P2：物理几何与安全验收

1. 最终STL尺寸/原点/装配/余量。
2. 工作空间、夹具/支架和静态自由区。
3. 已知安全/碰撞轨迹的连续证明。
4. 自遮罩、占用图、UNKNOWN壳和地图新鲜度。
5. 低速、在场、可急停的ServoJ包络验收。

### P3：几何科学和运行时序验收

1. 确定基准工作距离及最小/最大自适应包络。
2. 验收首帧掩模和schema-5参考掩模对两只鳍片和全部边界的保留率。
3. 评估双面RMSE、厚度、法向、孔洞和拓扑覆盖。
4. 生成新主机science runtime contract和独立复核资产。
5. 在同一boot/workcell实测四类时序trace，生成timing acceptance。

### P4：完整叶片闭环

1. `scan doctor --mode unknown`通过。
2. 人工三个独立初始视图建图。
3. 粗扫正反面各至少三视图。
4. 生成并封存schema-5。
5. 完成主表面、全边界和两只鳍片的精扫。
6. 达到`FINE_COMPLETED`，严格重放事件链并导出最终几何指标。
7. 根据实验数据回写论文，不用未实测默认值作为实验结论。

### P5：热像和论文完善

只在几何闭环稳定后接入热像仪。更换末端装配后重建最终STL、手眼/外参、碰撞和时序验收。论文目录为相邻的`../latex-robot-scene`；旧PC-NBV叙事已废弃，不应恢复。

## 14. 命令运动能力分类

| 命令 | 接触硬件 | 可能运动 |
|---|---:|---:|
| Ruff、pytest、compileall | 否 | 否 |
| `bbf stereo doctor/infer-session` | 仅GPU/保存资产 | 否 |
| `bbf doctor` | 可枚举RealSense，不开流；不连ES68 | 否 |
| `bbf scan doctor` | 否 | 否 |
| `bbf robot inspect-model` | 否 | 否 |
| `bbf supervise replay` | 否 | 否 |
| `bbf camera list/capture` | D435i | 否 |
| `bbf robot status/export-kinematics` | ES68只读接口 | 否 |
| `bbf acquire snapshot` | D435i + ES68只读状态 | 否 |
| `bbf occupancy build-replay/inspect` | 否 | 否；回放图固定为`STALE` |
| 标定GUI | 相机/ES68只读状态 | 程序不运动，人用示教器移动 |
| `bbf safety ...` | 否 | 否 |
| `bbf commission motion-envelope-trial` | dry-run否；`--execute`接触ES68 | **仅`--execute`是；单候选且最大0.02 rad** |
| `bbf scan run-unknown` | D435i + ES68 | **是，唯一公开闭环运动入口** |
| `bbf scan run-unknown --resume` | D435i + ES68 | **是，除非已封存完成态** |

## 15. 新主机接收完成标准

以下项目全部满足后，才可称“迁移接收完成”：

- [ ] 主仓库和FoundationStereo子模块身份已记录；
- [ ] 所有非Git数字资产已通过SHA-256校验；
- [ ] `configs/local.yaml`已完整复制并审查，不是残缺覆盖片段；
- [ ] FoundationStereo checkpoint和相邻`cfg.yaml`存在；
- [ ] GPU bootstrap通过，Elite SDK仍可导入；
- [ ] 986项离线测试通过；
- [ ] 保存session的FoundationStereo完整CUDA推理通过；
- [ ] D435i 1280×720左右红外采集通过；
- [ ] ES68只读状态和控制器MDH导出通过；
- [ ] D435i + ES68同步快照及其FoundationStereo推理通过；
- [ ] STL装配离线可视化正确；
- [ ] 新主机 Codex 已明确列出尚未完成的物理验收，没有报告自动运动已可用；
- [ ] 本文档和现场新生成的身份/日志已纳入一次独立Git提交。

## 16. 权威参考

- [README](README.md)
- [未知叶片监督式闭环实验](docs/supervised-blade-experiment.md)
- [FoundationStereo单后端停—算—规划—短移协调器](docs/stop-and-capture-coordinator.md)
- [占用图运动安全](docs/occupancy-motion-safety.md)
- [碰撞验证](docs/collision-validation.md)
- [D435i红外双目标定](docs/stereo-calibration.md)
- [手眼标定与坐标约定](docs/calibration.md)
- [双面曲面重建](docs/curved-reconstruction.md)
- [最终精扫重建](docs/fine-final-reconstruction.md)
- [覆盖与下一视点](docs/coverage-next-view-selector.md)
- [机器人技术链迁移](docs/robot-stack-migration.md)
- [开发日志](docs/development-log.md)

---

本文档不是运动许可，不代替操作员、真实急停、现场风险评估、物理验收或每段唯一approval token。
