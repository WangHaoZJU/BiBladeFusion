# BiBladeFusion 当前接收与实验验收文档

> 更新日期：2026-09-04（Asia/Shanghai）
> 适用分支：`main`
> 基线：以包含本文档的提交为准；部署后必须记录 `git rev-parse HEAD`
> 当前结论：软件回归通过，下一次 eiai 真机验收尚未完成

本文档取代 2026-08-29 的旧迁移说明，是 vale、eiai 和后续 Codex 任务共同使用的
当前接收边界。它不把“代码通过”“环境通过”或“doctor 通过”写成真机运动已经完成。

## 1. 必须首先恢复的任务主线

项目面向 ES68 + D435i 对薄壁、双面、带鳍片叶片进行主动测量。论文以一套完整系统为主，
核心问题是：

> 从一个操作员选择的首视角和一个叶片 polygon 出发，主动搜索能提供有效新叶片/鳍片
> 信息、IK 可解，并且在当前停止姿态下存在安全完整机械臂路径的下一视点。

FoundationStereo 多视角重建和温度场映射是后续消费者，不能使项目偏离视点规划与可靠运动
这条主线。热像仪代码继续与 `main` 的几何规划工作隔离。

开始任何修改或实验前，必须完整阅读：

- `AGENTS.md`
- `docs/PROJECT_MEMORY.md`
- `docs/CURRENT_STATE.md`
- `docs/DECISION_LOG.md`
- `docs/EXPERIMENT_RUNBOOK.md`

一般机器人控制、状态采样、ServoJ、IK、占用和路径规划首先对照操作者拥有的
`~/Documents/HoloRobot`。只在 HoloRobot 不具备的薄壁双面鳍片表示、信息增益、ROI 身份传播
和科学覆盖层上新增项目专用逻辑。

## 2. 当前接受的端到端流程

```text
人工放置到安全首视角并证明停止
  -> 同步采集左右红外图和机器人状态
  -> FoundationStereo 深度
  -> 操作员在左矫正图绘制一次 blade polygon
  -> polygon 内建立叶片科学代理
  -> 机器人自遮罩后的全场景深度独立建立安全占用图
  -> 生成双面普通候选和鳍片发现候选
  -> 按预期新信息进行科学排序
  -> 从当前停止关节姿态重新求解 IK 分支
  -> 原始 URDF/STL 端点碰撞否决
  -> HoloRobot 直线路径优先
  -> 仅内部路径阻塞时运行有界 RRTConnect
  -> 完整采样路径通过网格和保守占用检查
  -> 精确人工 approval token
  -> 一次完整 ServoJ 视点运动
  -> 一次 writeIdle、停稳证明和自动采集
  -> 后续 ROI 由已接受叶片代理投影和前景/深度一致性自动产生
  -> 粗扫 schema-5 交接
  -> fine NBV、最终重建和 COMPLETE
```

IK、工作空间、端点碰撞、路径碰撞和证据完整性是硬否决条件，不增加科学得分。相机距离、
入射角、腕部 roll 和鳍片倾斜是搜索变量；`+-15 deg` 只能作为种子，不能成为僵硬门限。

安全占用图使用完整场景；叶片科学模型只使用叶片 ROI。两者不得互相替代。机器人自碰撞和
环境查询使用 ES68+D435i 的 URDF 与原始碰撞 STL，不允许恢复大包围球作为最终碰撞模型。

## 3. 本次软件接受内容

### 3.1 当前停止姿态下的视点可行性

- 普通叶面候选和鳍片候选都会在每个已接受停止姿态重新计算 IK 与端点碰撞。
- 首视角下不可达的候选不会永久被冻结；机械臂移动后可重新进入排序队列。
- HoloRobot Pinocchio/MDH 种子按确定顺序惰性检查；第一个端点安全分支即可结束。
- 鳍片候选按 side/axis 广度优先展开，避免前侧慢候选耗尽预算后仍未尝试背侧。
- 搜索结果区分完整遍历、候选数限制、IK 次数限制、可行候选数限制和时间限制；预算截断
  不能被报告为“物理上全部不可达”。
- 历史鳍片证据继续绑定到当时真正用于选择的不可变 discovery revision。

### 3.2 HoloRobot 对齐的运动路径

- 先完成目标原始网格检查，再完成 goal-first 的整条保守占用路径；只有占用路径 CLEAR 才扫描
  完整 mesh path。两个完整采样门均通过才可能批准运动，改变的只是 hard-veto 求值顺序。
- 只有端点均安全且直线路径内部阻塞时，才调用一次有界 RRTConnect。
- RRT 路径重新采样并完整复查，起点和终点 tuple 原样保留，避免浮点重算产生一 ULP 身份差。
- 在线占用路径只绑定/哈希一次不可变地图，并在首个阻断机器人网格的首个阻断 voxel 处立即
  失败；独立诊断模式仍可完整枚举并显式标识查询是否完整。
- ServoJ 继续使用 HoloRobot 的速度和加速度联合时间参数化、严格起点容差、终点反馈收敛及
  单次 `writeIdle` 停止边界。
- 没有放宽 UNKNOWN、净空、碰撞、跟踪、停止或人工批准阈值。

### 3.3 一次规划事务的响应预算

`stop_and_capture.maximum_planning_preflight_duration_s` 默认是 30 秒。现在它是从 coordinator
进入 planning 时开始的同一个绝对合作式 deadline，贯穿：

- 粗/细候选循环和 adaptive pose family；
- Pinocchio/MDH IK seed、数值迭代和分支端点碰撞；
- 直线路径网格/占用采样；
- 占用 robot-STL、voxel-run 和 HPP-FCL distance 循环；
- OMPL state validity、solve、simplify 和路径重采样。

OMPL 只得到“本地配置时间”和“总剩余时间”中的较小值。deadline 用尽进入可恢复的
`MOTION_BLOCKED`，不伪装成低增益、无 IK 或碰撞安全。

这不是线程强杀或硬实时保证。一次 Pinocchio/FCL/KDL/OMPL 原生调用、地图哈希、大型 NumPy
操作或机器人状态读取不能在中间安全终止，只能在返回后的第一个检查点发现超时。因此准确的
响应边界是“30 秒加最多一个不可分割原生调用及返回延迟”。

### 3.4 中断恢复

完整、可验证的实验链可以用 `--experimental --resume` 继续：

- 从所有已持久化事件之后分配新的 cycle/view 身份；
- 恢复已接受的粗扫或 fine 科学证据；
- 不恢复旧 proposal、approval permit、占用新鲜度、source window、stop latch 或进行中的运动；
- 先获得真实停止证据，再要求一次 occupancy-only `SAFETY_REFRESH`；
- 不重新要求首视角 hard ROI；
- `SAFETY_REFRESH` 不计入叶片科学视图；
- 物理叶片、夹具、相机装配或机器人基座发生变化时禁止 resume。

### 3.5 只读实验可视化

活动运行会原子更新 `<OUTPUT>/live_timeline/live_planning.json`。现有 PySide supervisor 显示：

- 科学排序、科学分数和候选队列；
- 当前/选中候选；
- IK、端点、直线、RRT 状态；
- 已有独立证据的阶段耗时和准确阻断原因；
- 当前相机黄色视锥，以及候选灰色、活动蓝色、选中绿色视锥；
- 机械臂/相机模型、占用图、当前点云、粗模型和事件时间线。

侧车和界面不重新执行 IK、碰撞或 RRT，也没有任何批准、运动或停止命令。观察器写入/读取
失败只能产生警告，不能中断科学流程或真机运动。没有独立证据的字段保持 `UNKNOWN/PENDING`。

## 4. 当前 eiai 配置和不可变接受资产

以下为操作者已经报告并由 `scan doctor` 成功读取的部署状态；它们不在 Git 中，拉取代码后
不得被默认配置覆盖。

```text
placement_id: blade-placement-20260901-01

workspace:
  x = [-1.00, 1.00] m
  y = [-0.55, 0.55] m
  z = [ 0.00, 1.10] m

blade science envelope:
  minimum = [0.454, 0.001, 0.031] m
  maximum = [0.639, 0.164, 0.368] m

static-free acceptance:
  path = data/acceptance/es68_d435i_static_free_002
  id   = b87a40387b6bcbb9e802c6a984edc8a883766d9c64a433ebe908d274b7965bae

motion-envelope acceptance:
  path            = data/acceptance/es68_d435i_motion_envelope_004
  id              = 29624b08242d2c8ef7544cb958bf2a64335f895b719b421f792ff8c750719f9b
  metadata SHA256 = ce807010bc11bdf50bcfb804214f85705ad155bf3c7af16d3a214a2354722766
```

其他已确认配置：adaptive IK 开启；单视角 accepted-static-free bootstrap motion 开启；占用图
使用 CUDA DDA；ES68 加速度向量为 `[4,4,4,4,4,4] rad/s^2`；正式深度后端仅为
FoundationStereo。运行 Python 3.12 环境时必须使用 `/usr/bin/env -u PYTHONPATH`，避免 ROS
Humble 的 Python 3.10 Pinocchio/HPP-FCL 覆盖虚拟环境。

## 5. 与最终目标仍有差距的接受边界

以下事项不得因为本次提交或离线测试通过而写成已完成：

1. **真机首段已通过，后续链仍待验证。** D038 已在 eiai 完成“首视角 -> NBV ->
   ServoJ -> 一次 writeIdle/停稳 -> 自动第二帧采集”；第三安全源、后续视点、双面/双鳍片、
   schema-5、fine 和最终 COMPLETE 仍未获得真机证据。
2. **逐段人工批准仍存在。** 每个 planned segment 需要精确 `EXECUTE` token；这是当前实验
   调试控制点，不是最终全自动系统状态。
3. **在线不是递归连续证书。** 按操作者决定，在线运动已经替换为 HoloRobot 固定步长的
   完整路径采样检查。递归 interval/swept-volume 证明只保留为离线诊断和接受工具。论文不得
   把在线结果描述为数学连续碰撞证书。
4. **30 秒不是硬实时强杀。** deadline 是合作式的，存在一个不可分割原生调用的超量边界。
5. **远程浏览器可视化未完成。** 当前是 PySide；SSH X11 可能卡顿。下一阶段只应抽取
   HoloRobot 的只读 HTTP/SSE + Three.js 部分，禁止复制控制路由。
6. **重复 IK/端点复核已按强绑定消除。** 只有 discovery policy、精确停止关节、typed generation
   authority 和 endpoint validator 全部一致时才复用既有结果；任一不一致都回退完整 rebind。
   该优化不缓存 path preflight，也不改变 collision/UNKNOWN veto。
7. **后续 ROI、双面/双鳍片覆盖、schema-5、fine 重建和最终 COMPLETE 仅有软件组合回归。**
   仍需真实叶片逐步验证掩模身份、覆盖质量和几何结果。
8. **热像与温度场映射不属于当前几何运动验收。** 不得混入本次 main 提交。

## 6. 本次代码验证记录

当前 D039 工作树统一验证：

```text
focused storage/occupancy/runtime/planning regression: 552 passed in 35.04 s
full repository regression: 1337 passed, 1 skipped in 176.22 s
ruff: all checks passed
git diff --check: passed
```

1 个 skip 是测试进程未暴露 CUDA 的 CUDA-only 单元用例。测试未打开机械臂或相机，因此不能
替代 FoundationStereo、控制器和真实运动验收。

主机 CUDA 对 D038 generation `000001` 的 append-only handoff 只读验证为 `65.401 s`，恢复
phase=`coarse`、`experimental_unaccepted=true`，且没有伪造 science/timing authority。selector
从实测 `45.89 s` 降到 `5.45-5.59 s`，11 项排序与 TCP 目标不变。完整无时限诊断确认这张
两视角 `MAPPING` 图上不存在安全自动路径：前 10 项被 UNKNOWN occupancy 否决，第 11 项的
straight/RRT occupancy 均 CLEAR，但两条原始 mesh 路径均发生 forearm-D435i 自碰撞。

## 7. eiai 接收顺序

### 7.1 拉取和环境保护

```bash
cd /home/eiai/Documents/wh/BiBladeFusion
git status --short --branch
git pull --ff-only origin main
git log -1 --oneline
git status --short --branch
```

不得例行执行 `uv sync --frozen`；它曾移除私有 Elite SDK 和 GPU/FoundationStereo 部署依赖。
若确需同步依赖，只使用项目 GPU bootstrap 过程及真实 Elite SDK wheel。

### 7.2 非运动检查

```bash
/usr/bin/env -u PYTHONPATH .venv/bin/bbf \
  scan doctor \
  --mode unknown \
  --experimental \
  --ray-integration-backend cuda \
  --config configs/local.yaml
```

必须通过 Elite SDK、FoundationStereo、CUDA、CUDA DDA、RealSense、标定、静态自由接受、
运动包络、原始 URDF/STL 后端、HoloRobot single-arm planner 和 coarse-to-fine 检查。xFormers
与 FlashAttention 仍是可选加速警告。

### 7.3 真机实验

完整新运行、首视角标注、可选 ROI 复用、实验 resume 和失败证据收集命令统一以
`docs/EXPERIMENT_RUNBOOK.md` 为准。D038 已用新 `run_id` 完成首段真机验证；D039 必须续接其
不可变 checkpoint，不应另起输出重复第一段。

另一个终端运行只读 supervisor：

```bash
/usr/bin/env -u PYTHONPATH .venv/bin/bbf \
  supervise replay \
  --snapshot /absolute/run/output/live_timeline \
  --follow
```

## 8. 本 revision 的真机通过条件

第一次 eiai 验收至少必须同时满足：

- 首视角 hard ROI 只绘制一次并产生有效叶片 proxy；
- 全场景占用图与叶片 ROI 保持独立；
- planning UI/sidecar 能指出正在处理的候选和阶段耗时；
- 规划在正常测得时间内到达 `waiting_approval`，或给出准确、可恢复的 typed blocker；
- 候选由当前停止姿态 IK 产生，端点和完整路径均通过原始网格与占用检查；
- 输入精确 token 后只建立一次有效 reverse-control/ServoJ 执行链；
- 机械臂按显示路径完成一个完整视点运动，无异常跳变；
- 到达目标后完成终点反馈、一次 `writeIdle` 和停稳证明；
- 自动采集下一帧，不要求重新绘制 hard ROI；
- 运动、停止、采集、占用更新和下一个 planning 周期均被不可变事件链记录；
- 只读 GUI 关闭或故障不会改变实验结果。

任何一项失败都不能笼统归因于“机械臂不可达”。必须保存候选 ID、科学分、IK/端点结果、
路径类型、阻断 link/voxel、阶段耗时、机器人模式、runtime state、占用绑定和最后事件 SHA。

## 9. 停止和重新试验规则

出现以下任一情况立即停止，不继续输入 approval token：

- 配置、接受 ID、标定或碰撞模型 hash 不匹配；
- 当前机械臂姿态与预检起点不一致；
- 叶片、夹具、相机装配或基座发生未记录移动；
- GUI 中目标视点/路径与真实工作区明显不一致；
- 端点或路径状态为 UNKNOWN/BLOCKED；
- 机器人控制器、反向端口、tracking 或 stop/stationarity 状态异常；
- 线缆拉扯或存在当前模型没有覆盖的障碍。

失败后先保留完整 output、终端日志和 `live_timeline`，完成代码级原因与离线回归后再启动新
run。当前“代码可接受”的准确结论不能替代操作者在场、低速和可直接急停的真机条件。

## 10. D034 运动修复后的当前接受状态

`planning-fix-d033-20260904-201710` 已在真机上通过规划、路径预检和精确审批，
但执行在 `tracking_error_exceeded` 后安全停止，没有自动采集。故本文档前文的
“首次真机 NBV 待验证”已更新为：规划链已真机通过，运动跟踪链未通过。

只读 RTSI 复核确认机械臂位于 `NORMAL / STOPPED / 六轴速度为零`，实际只到达旧
4 ms 路径约 `0.187 s` 的位置。D034 改用同一 Elite-A 在 HoloRobot 中已有真机
证据的 `8 ms` ServoJ 周期、`0.03 s` lookahead、`1.0` Dashboard speed scaling
和 `0.05` 轨迹时间缩放，并将点流
改为真正的零速起停梯形/三角形剖面。同一旧目标的新离线点流为 `10.888 s`，
J6 峰值速度约 `0.160518 rad/s`，起止首末区间速度约 `0.000640 rad/s`。

`tracking_error_rad=0.03` 保持不变。旧 `_003` 接受资产继续保存但不再激活；
在当时的 D034 阶段，`configs/local.yaml` 中 motion-envelope path/ID 被置为 null，因此
不允许直接重跑长行程。
必须先按 `docs/EXPERIMENT_RUNBOOK.md` 完成新控制合同的有界 commissioning 与不可变
接受，再启动新 `run_id` 的端到端试验。D034 离线验证为聚焦回归
`141 passed`，全仓回归 `1306 passed, 1 skipped`（该 skip 为测试进程无 CUDA）。

首次 D034 commissioning 执行在创建 EliteDriver 前失败：`xray` 的本地回环
连接恰好使用了处于 Linux 临时端口范围内的 `50002`。该试验证据中
`before_state=null` 且 `stream_result=null`，所以没有机械臂运动。eiai 部署的
EliteDriver 四路本地端口现迁移为临时范围之外的 `29001–29004`；失败输出
保留不变，重试必须使用新输出路径和新审批 token。

D035 端口修改已由下一次真机连接和 ServoJ 发送验证。该次不可变证据为
`data/acceptance/d034_20260904-204620_trial_01_forward_retry01`：主机 8 ms 循环无
overrun，最大 tracking error 为 `0.018453 rad`，但 `writeIdle` 前机器人仍以最高
`0.009050 rad/s` 运动，最终越过旧目标并在目标外 `0.013553 rad` 停稳，因此试验
正确判定 FAIL，正式运动仍未授权。

D036 已纠正 D034 对两种 scaling 的混用：HoloRobot 的同一 Elite-A 实机登记值是
Dashboard `default_speed_scaling=1.0`，而轨迹时间缩放才是 `0.05`。commissioning
也不再把固定 1 s endpoint 当成普通点流；成功发送 rest-to-rest 路径后，会在
`writeIdle` 前调用与生产执行相同的反馈式 endpoint hold。停机后的 `0.002 rad`、
速度和连续一秒停稳门槛均未放宽。新 motion-control hash 为
`3a85600d873cd05eb7738a96a832c0a216c08d5da03b851f284b1aa10016db30`。

机器人当前停止位姿既不等于旧 candidate 起点，也不等于旧终点，所以禁止执行旧
candidate 的 forward 或 reverse。下一次必须从当前停止位姿重新采集 stereo 和全场
occupancy、生成新 candidate，再用新 output 和新 token 做 D036 forward commissioning。
D036 离线验证为相关测试 `119 passed`、全仓 `1305 passed, 1 skipped`，且 ruff 与
`git diff --check` 通过；这仍不是 D036 真机通过结论。

D036 forward commissioning 随后已在真机 PASS，对应不可变证据：
`data/acceptance/d036_20260904-211858_trial_01_forward`。90 条点流的最大 tracking
error 为 `0.003168 rad`；`writeIdle` 前反馈式 endpoint hold 最终误差为
`0.00003185 rad`；最大 stop drift 为 `0.00006060 rad`；停机后连续稳定窗口为
`1.009667 s`，最终目标误差 `0.00003223 rad` 且关节/TCP 速度为零。这验证了 D036
的一次有界 forward 控制边界，但 production motion 仍未授权；reverse、故障 stop
试验和新 motion-envelope acceptance 仍待完成。

首次 reverse 尝试
`data/acceptance/d036_20260904-211858_trial_02_reverse` 在 ServoJ 之前失败，
`stream_result=null` 且机器人前后关节完全相同。原因是 forward 的正常停止残差
`0.00003223 rad` 虽满足 `0.001 rad` 起点容差，却让“实时起点到精确旧起点”的
reverse 距离变成 `0.02003223 rad`，暴露了 live-start 容差与精确 `0.02 rad` trial
上限之间未闭合的代码合同。

D037 不放宽这两个数值。它从实时停止姿态沿封存终点方向裁剪出不超过 candidate
自身上限的实际终点，再对该精确 live path 重新生成零速起停点流并执行完整原始 mesh
proof。使用失败态精确重放得到 `0.02000000 rad`、90 条、`0.712 s`、CLEAR、连续
proof 有效、最小证书余量 `0.004175745 m`。相关测试 `125 passed`，全仓
`1306 passed, 1 skipped`，ruff 和 `git diff --check` 通过。失败尝试没有移动机器人，
同一 candidate 可用新 output/token 重试 reverse；production motion 仍未授权。

D037 reverse 重试随后已在真机 PASS，不可变证据为
`data/acceptance/d037_20260904-211858_trial_02_reverse_retry01`。实时请求距离
`0.02003223 rad` 被严格裁剪为 `0.02000000 rad`，90 条 ServoJ 最大 tracking error
为 `0.003236 rad`；endpoint hold 最终误差 `0.00002164 rad`，最大 stop drift
`0.00005387 rad`，并完成 `1.009718 s` 的零关节/TCP 速度稳定窗口。最终关节反馈
精确回到 candidate 原始封存起点。forward/reverse nominal 试验均已完成，但这仍不
代表 production motion 获得授权。

在执行最后的 intentional tracking-fault 试验前发现，旧的十条命令窗口与新的
rest-to-rest 曲线不相容：两次 nominal 真机数据在前十条内的最大误差都只有
`0.00040904 rad`，而真实 `0.001 rad` 故障阈值首次到 index 15 才被越过。D038 不降低
该故障阈值、不注入虚假反馈，也不修改 production 的 `0.03 rad` tracking guard；仅让
故障试验逐条检查，最多发送 24 条。当前 0.01-rad 故障轨迹的前 24 条命令最大位移为
`0.00266566 rad`，执行前还强制该窗口落在 `[0.002, 0.003] rad` 并将实际值写入证据。
故障试验必须在 outer trial 中表现为 PASS、inner stream 原因为
`tracking_error_exceeded`、命令数不超过 24 且停稳窗口通过，才能用于新 acceptance。

D038 真机试验已经满足上述全部条件：第 16 条命令由真实 RTSI 反馈触发
`tracking_error_exceeded`，最大误差 `0.00101258 rad`，stop acknowledgement
`0.00003152 s`，最大 stop drift `0.00043768 rad`，随后完成 `1.010317 s` 零速度
稳定窗口。故 commissioning 到此结束，不再追加试验。

新的不可变 motion-envelope acceptance 为
`data/acceptance/es68_d435i_motion_envelope_004`，ID
`29624b08242d2c8ef7544cb958bf2a64335f895b719b421f792ff8c750719f9b`，metadata SHA-256
为 `ce807010bc11bdf50bcfb804214f85705ad155bf3c7af16d3a214a2354722766`。它保留旧有
代表性工作区证据中更大的逐轴 tracking 上界，并取新旧证据逐轴最大的 stop drift，
安全系数仍为 1.5；没有用短行程结果缩小安全包络。`configs/local.yaml` 已绑定该资产，
并恢复 production motion 与 stop-and-capture。该段记录中的“下一步新 run”已经由 D038
unknown-blade 首段完成；当前入口以第 11 节的 D038 resume 为准，不再 commissioning。

## 11. D039 NBV 首段结果与明日验收入口

D038 unknown-blade 输出
`data/experiments/blade-placement-20260901-01-real-nbv-d038-20260904-220200` 已完成第一段
真实 NBV：1,342 条 ServoJ 命令、`13.236086 s`，停稳窗口 `1.039144 s`，最终目标误差
`0.00002409 rad`，最大关节漂移 `0.00005387 rad`。随后 FoundationStereo、ROI 传播、全场景
occupancy 和第二个 coarse generation 均成功落盘，没有再次要求 hard ROI。

本次修复不再把 planning deadline 伪装成“请手动移动后继续采图”。deadline 会形成
`planning_restart_required` 并干净退出；恢复时验证整个 append-only 链，丢弃旧 proposal、
permit 和 freshness，只保留不可变 science checkpoint。消费前的大文件校验改为 typed
storage authority 的逐文件 SHA/size/header 复核，仍检查 metadata、全部 arrays、snapshots、
stereo/session/hand-eye，且不同路径的相同内容不会被错误去重。

明日只执行 `docs/EXPERIMENT_RUNBOOK.md` 第 7 节中的 D038 literal resume 命令。前提是叶片、
夹具、相机装配和机器人基座均未改变，且机械臂当前已停止。resume 验证停止位姿后不要再移动，
只输入一次 `c`，取得第三个独立 occupancy-only safety source；不要输入 `c front/back`，不要
重新标注 ROI。只有新规划到达 `waiting_approval`、GUI 路径与现场一致时，才粘贴新生成的精确
token。第二段真机运动及其后的 coarse-to-fine 流程仍是待验收项，本文档不将其写成已通过。
